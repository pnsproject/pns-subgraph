type Domain @entity {
  id: ID!
  name: String!
  node: BigInt!
  owner: Bytes! # address
  cost: BigInt! # uint256
  expires: BigInt!
}

type Subdomain @entity {
  id: ID!
  tokenId: BigInt!
  name: String!
  owner: Bytes!
}
